---
import { landing } from "../../content/landing";
const s = landing.faq;
---

<section class="lp-band lp-band--paper lp-faq-section" aria-labelledby="lp-faq-h">
  <div class="lp-container lp-container--narrow">
    <div class="lp-faq-header lp-fade-in" data-viz>
      <span class="lp-eyebrow">Questions</span>
      <h2 id="lp-faq-h" class="lp-h2">FAQ</h2>
    </div>
    <div class="lp-faq-list lp-fade-in" data-viz style="transition-delay: 0.1s;">
      {
        s.items.map((item) => (
          <details class="lp-faq-item" name="landing-faq">
            <summary class="lp-faq-q">{item.q}</summary>
            <p class="lp-faq-a">{item.a}</p>
          </details>
        ))
      }
    </div>
  </div>
</section>
