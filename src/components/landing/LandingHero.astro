<section class="lp-hero" id="hero">

  <!-- Layered background: gradient mesh + dot grid -->
  <div class="lp-hero-bg" aria-hidden="true">
    <div class="lp-hero-mesh"></div>
    <div class="lp-hero-dots"></div>
  </div>

  <!-- Single centered content column -->
  <div class="lp-hero-body">

    <span class="lp-hero-eyebrow">Strategic Personal Branding</span>

    <h1 class="lp-hero-h1">
      Your expertise closes deals.<br>
      <span class="lp-hero-h1-accent">Your brand should open them.</span>
    </h1>

    <p class="lp-hero-sub">
      For entrepreneurs who've outgrown word of mouth, and need the market
      to catch up with what their clients already know.
    </p>

    <ul class="lp-hero-adv" aria-label="Key advantages">
      <li>Inbound leads from markets that have never heard your name</li>
      <li>A brand position that sells before you enter the room</li>
      <li>Shorter sales cycles with prospects who arrive pre-sold</li>
      <li>Authority that compounds like equity</li>
    </ul>

    <a href="/contact" class="lp-hero-cta">
      Book a Strategy Call
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
        <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"
          stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </a>

  </div>

  <!-- Subtle scroll cue -->
  <div class="lp-hero-cue" aria-hidden="true"></div>

</section>

<style>
/* ─── Hero — all styles self-contained, no class conflicts ───────────────── */

.lp-hero {
  /* Section fills body width. Body.is-landing has no max-width. */
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100svh;
  padding: clamp(7rem, 12vw, 11rem) clamp(1.5rem, 5vw, 3.5rem) clamp(5rem, 8vw, 8rem);
  position: relative;
  overflow: hidden;
  text-align: center;
}

/* Background layers */
.lp-hero-bg { position: absolute; inset: 0; pointer-events: none; }

.lp-hero-mesh {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 65% 55% at 80% 10%, var(--mesh1, rgba(79,143,247,0.22)) 0%, transparent 65%),
    radial-gradient(ellipse 55% 50% at 10% 85%, var(--mesh2, rgba(129,140,248,0.14)) 0%, transparent 60%);
}

.lp-hero-dots {
  position: absolute;
  inset: 0;
  background-image: radial-gradient(var(--dot, rgba(79,143,247,0.06)) 1px, transparent 1px);
  background-size: 28px 28px;
  opacity: 0.5;
}

/* Content column — max-width just controls line length, section is still 100% wide */
.lp-hero-body {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.75rem;
  max-width: 860px;
  width: 100%;
  animation: lp-hero-in 0.8s cubic-bezier(0.22, 1, 0.36, 1) both;
  animation-delay: 0.05s;
}

@keyframes lp-hero-in {
  from { opacity: 0; transform: translateY(28px); }
  to   { opacity: 1; transform: none; }
}

/* Eyebrow */
.lp-hero-eyebrow {
  display: inline-block;
  font-family: var(--fB, "Inter", sans-serif);
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent, #4F8FF7);
}

/* Main headline */
.lp-hero-h1 {
  font-family: var(--fH, "Satoshi", sans-serif);
  font-weight: 800;
  font-size: clamp(2.75rem, 6vw, 5.5rem);
  line-height: 1.06;
  letter-spacing: -0.04em;
  color: var(--t1, #DCE4FF);
  margin: 0;
}

.lp-hero-h1-accent {
  color: var(--accent, #4F8FF7);
}

/* Sub headline */
.lp-hero-sub {
  font-family: var(--fB, "Inter", sans-serif);
  font-size: clamp(1rem, 1.5vw, 1.175rem);
  line-height: 1.75;
  color: var(--t2, #7A8DB5);
  max-width: 52ch;
  margin: 0;
}

/* Advantages grid */
.lp-hero-adv {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.625rem;
  width: 100%;
  max-width: 700px;
  text-align: left;
}

@media (max-width: 560px) {
  .lp-hero-adv { grid-template-columns: 1fr; }
}

.lp-hero-adv li {
  display: flex;
  align-items: flex-start;
  gap: 0.625rem;
  font-family: var(--fB, "Inter", sans-serif);
  font-size: 0.9rem;
  color: var(--t2, #7A8DB5);
  line-height: 1.55;
  padding: 0.75rem 1rem;
  background: var(--gBg, rgba(11,14,28,0.6));
  border: 1px solid var(--gB, rgba(255,255,255,0.09));
  border-radius: 12px;
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
}

.lp-hero-adv li::before {
  content: '';
  flex-shrink: 0;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent, #4F8FF7);
  margin-top: 6px;
  opacity: 0.75;
}

/* CTA button */
.lp-hero-cta {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1.0625rem 2.75rem;
  font-family: var(--fB, "Inter", sans-serif);
  font-size: 1rem;
  font-weight: 600;
  color: #fff;
  background: var(--accent, #4F8FF7);
  border: none;
  border-radius: 999px;
  text-decoration: none;
  cursor: pointer;
  transition: background 0.25s, transform 0.2s;
  margin-top: 0.5rem;
}

.lp-hero-cta:hover {
  background: var(--accentH, #6FA5FF);
  transform: translateY(-2px);
}

/* Scroll cue */
.lp-hero-cue {
  position: absolute;
  bottom: 2.5rem;
  left: 50%;
  transform: translateX(-50%);
  width: 1px;
  height: 48px;
  background: linear-gradient(to bottom, transparent, var(--accent, #4F8FF7), transparent);
  animation: lp-cue-pulse 2.5s ease-in-out infinite;
}

@keyframes lp-cue-pulse {
  0%, 100% { opacity: 0.15; }
  50%       { opacity: 0.55; }
}

@media (prefers-reduced-motion: reduce) {
  .lp-hero-body { animation: none; opacity: 1; transform: none; }
  .lp-hero-cue  { animation: none; }
}
</style>
